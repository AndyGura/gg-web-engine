{
  "name": "gg-web-engine",
  "private": true,
  "description": "Monorepo root for @gg-web-engine/* packages — not published. See CLAUDE.md and the gg-engine-* skills for how packages/* and examples/* relate.",
  "workspaces": [
    "packages/*",
    "e2e/*/app"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run prettier-format && npm run build --workspaces --if-present",
    "build:watch": "tsc -b --watch",
    "clean": "tsc -b --clean",
    "test": "npm run test --workspace=packages --if-present",
    "prettier-format": "npm run prettier-format --workspaces --if-present"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^26.4.0",
    "jest": "^30.5.0",
    "jest-environment-jsdom": "^30.5.0",
    "ts-jest": "^29.4.12",
    "typescript": "~6.0.3"
  }
}
